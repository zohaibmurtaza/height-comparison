export enum ItemType {
  PERSON = "person",
  OBJECT = "object",
  BUILDING = "building",
  ANIMAL = "animal",
  IMAGE = "image",
}

export enum Gender {
  MALE = "male",
  FEMALE = "female",
}

export enum BodyType {
  ECTOMORPH = "ectomorph",
  MESOMORPH = "mesomorph",
  ENDOMORPH = "endomorph",
}

export enum AvatarCategory {
  ADULT = "adult",
  CHILD = "child",
  PET = "pet",
}
